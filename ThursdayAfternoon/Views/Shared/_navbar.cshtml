@using ThursdayAfternoon.Nancy.Extensions

<div class="navbar">
    <div class="navbar-inner">
        <div class="container">
            <button type="button" class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="brand" href="@Url.Content("~/")">Project name</a>
            <div class="nav-collapse collapse pull-right">
                <ul class="nav">
                    <li class="active"><a href="@Url.Content("~/")">Home</a></li>
                    <li><a href="@Url.Content("~/presentation")">Presentations</a></li>
                    @if (Html.IsAuthenticated)
                    {
                        <li class="dropdown">
                            <a href="#" class="dropdown-toggle" data-toggle="dropdown">@Html.User().UserName <b class="caret"></b></a>
                            <ul class="dropdown-menu">
                                <li><a href="#">Settings</a></li>
                                <li class="divider"></li>
                                <li><a href="@Url.Content("~/logout")">Logout</a></li>
                            </ul>
                        </li>
                    }
                    else
                    {
                        <li><a href="@Url.Content("~/login")">Login</a></li>
                    }
                </ul>
            </div>
            <!--/.nav-collapse -->
        </div>
    </div>
</div>
