@*@using ThursdayAfternoon.Nancy.Extensions
@inherits NancyRazorViewBase<ThursdayAfternoon.ViewModels.Presentation.EditViewModel>*@
@{
    Layout = "shared/_layout";
    ViewBag.Title = "Edit Slide";
}

<h1>Edit Slide</h1>

@*<div class="row">
    <div class="col-lg-8">
        <form class="@Url.Content("~/presentation/edit/" + @Model.Id)" method="post">
            <div class="form-group">
                <label for="Title">Title</label>
                @Html.TextBox("Title", "form-control", "Title")
            </div>

            <div class="form-group">
                <label for="Description">Description</label>
                @Html.TextBox("Description", "form-control", "Description")
            </div>

            @Html.Hidden("CreatedOn")
            @Html.Hidden("ModifiedOn")
            @Html.Hidden("OwnerId")

            <button class="btn btn-default" type="submit">Edit</button>
            <a href="@Url.Content("~/presentation")" class="btn">Cancel</a>
        </form>
    </div>
    <div class="col-lg-4">
        <p>Slides: --</p>
    </div>
</div>*@
